# Runtime dependencies. Floors are the oldest release that installs and runs on
# Python 3.10 through 3.12, the range the test workflow covers.
streamlit>=1.27.0,<2.0
neo4j>=5.0.0,<6.0
anthropic>=0.40.0,<1.0  # earlier releases install, then raise against httpx 0.28
pandas>=2.1.1,<4.0
numpy>=1.26.0,<3.0
scipy>=1.11.2,<2.0
python-dotenv>=1.0.0,<2.0
openpyxl>=3.1.0,<4.0
pyarrow>=14.0.0

# Tests only: pytest>=7.0.0,<9.0